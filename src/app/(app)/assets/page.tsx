import { FolderOpen } from "lucide-react";
import { ComingSoon } from "@/components/misc/ComingSoon";

export default function AssetsPage() {
  return (
    <ComingSoon
      icon={FolderOpen}
      title="Aset Saya"
      description="Galeri semua hasil generate (gambar, video, banner, logo, foto produk) akan segera hadir di sini."
    />
  );
}
